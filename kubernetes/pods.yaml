apiVersion: v1
kind: Pod
metadata:
  name: myapp
  labels:
    name: myapp
spec:
  containers:
  - name: myapp
    image: nginx
    ports:
    - containerPort: 80
      protocol: TCP

  - name: tom
    image: tomcat
    ports: 
    - containerPort: 8080
      protocol: TCP